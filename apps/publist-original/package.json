{
  "name": "publist",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "release": "rm -rf zip/* && cp -r dist/* zip/ && cp config.json zip/ && cd zip/ && zip publist.zip * assets/*"
  },
  "dependencies": {
    "sass": "^1.34.1",
    "sass-loader": "^12.1.0",
    "vue": "^3.0.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.1.4",
    "@vue/compiler-sfc": "^3.0.5",
    "vite": "^2.9.13"
  }
}
